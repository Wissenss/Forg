# this file defines helpers to read / write a value to the configuration table

import database

# TODO
def read_value(connection, name, account_id, default : str):
    return

# TODO
def set_value(connection, name, account_id, value : str):
    return